class Solution {
public:
    int missingNumber(vector<int>& arr) {
               sort(arr.begin(),arr.end());
               int start=0;
               for(int i=0;i<arr.size();i++){
                if(arr[i]!=start){
                    return start;
                }
                start++;
               }
               return start;
    }
};







